
  # Pairwise Voting Screen (Copy)

  This is a code bundle for Pairwise Voting Screen (Copy). The original project is available at https://www.figma.com/design/XlnK148AhND5iWNsk97jFW/Pairwise-Voting-Screen--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  