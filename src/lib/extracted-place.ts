/** Shared shape for UI place rows (import sheet + extraction). */
export type ExtractedPlace = {
  id: number;
  name: string;
  category: string;
  emoji: string;
  duration: string;
  checked: boolean;
};
